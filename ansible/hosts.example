# Place in /etc/ansible/hosts
# This defines your local system.
# Replace "sites.mydomain.com" with the FQDN of this server.
# The group operations_host_ddev is used in playbook.yml to identify servers that should have DDEV installed.

[operations_host_ddev]
sites.mydomain.com ansible_connection=local
