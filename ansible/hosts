#
# Place in /etc/ansible/hosts
# This defines your local system.
# replace "localhost" with the FQDN of this server.
# (optional. This just puts it in the ansible output, which is nice.)
#

localhost ansible_connection=local
